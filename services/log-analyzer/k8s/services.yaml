apiVersion: v1
kind: Service
metadata:
  name: log-analyzer
  labels:
    app: log-analyzer
spec:
  type: ClusterIP
  selector:
    app: log-analyzer
  ports:
    - name: http
      port: 7000
      targetPort: 7000
      protocol: TCP
