apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: gateway
  labels:
    app: gateway
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: http
      port: 7500
      targetPort: http
      protocol: TCP
